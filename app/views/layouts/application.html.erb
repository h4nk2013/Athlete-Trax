<!DOCTYPE html>
<html>
<head>
  <title>Athlete-Trax | <%= yield(:title) %></title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>
  <div class="container">
    <%= render 'layouts/header' %> 
    <% if current_user %>
      Logged in as <b><%= current_user.email %></b>, 
     <a href="<%= current_user.docu.url %> "><b> submitted form</b>.</a></br>
     <%= link_to "Log out", log_out_path, class: "btn btn-mini btn_primary" %>
   <% else %>
      
      <% flash.each do |name, msg|%>
     <div class="alert"><%= content_tag :div, msg, :id => "flash_#{name}" %></div>
   <% end %>
   <%= yield %>
   <%= render 'layouts/footer' %>
  </div>
<% end %>
</body>
</html>
